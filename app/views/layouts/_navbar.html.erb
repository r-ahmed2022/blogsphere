 <nav class="navbar navbar-expand-lg bg-success d-flex navbar-custom" >
          <div class="container-fluid">
            <%= link_to 'Blogsphere', '/', class: 'display-1 text-white navbar-logo' %>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                  <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                                    
                  </ul>
                <% if controller_name == 'users' && action_name == 'index' %>

                    <form class="d-flex" role="search" action="/users" method="get">
                        <input id="search" class="form-control mr-sm-2" type="search" placeholder="Search users" aria-label="Search">
                    </form>
                <% end %>
            </div>
          </div>
</nav>